---
title: Blog
layout: "layouts/baseof.njk"
tags: "menuMain"
---

<main class="mb-12">
  <div class="flex flex-row flex-wrap justify-around">
    <div class="w-full md:w-1/2 p-2 mb-6">
      <h1 class="font-display font-bold text-6xl mb-2 leading-tight">
        {{ title }}
      </h1>
      <ul>
        {% for item in collections.blog %}
        <li class="mb-4 py-4">
          <header class="mb-4 border-t-2 border-b-2 border-gray-700 p-2">
            <h2 class="text-4xl font-display font-bold leading-tight">
              {% if item.permalink != false %}
              <a href="{{ item.url }}" class="hover:text-green-800">
                {{ item.data.title }}
              </a>
              {% else %}
              {{ item.data.title }}
              {% endif %}
            </h2>
            <small>posted on {{ item.date | readableDate}}</small>
          </header>
          <div class="markdown mb-4 p-2">
            {{ item.data.excerpt }}
            </p>
        </li>
        {% endfor %}

      </ul>
    </div>
    <div class="w-full md:w-1/3 p-4 mb-6">
      {% include "partials/sidebar.njk" %}
    </div>
  </div>
</main>
